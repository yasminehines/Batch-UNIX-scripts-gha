name: Test
on:
  push:
    branches:
      - master

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: npm install
      run: npm install
        
    - name: Overwrite Files & Directories
      run: |
        cd ./artifacts2.0
        mkdir -p Backup
        git mv *.txt /home/runner/work/Batch-UNIX-scripts-gha/Batch-UNIX-scripts-gha/artifacts2.0/Backup

    - name: Overwrite Files & Directories 2
      run: |
        cd ./artifacts2.0/Backup
        ls -R